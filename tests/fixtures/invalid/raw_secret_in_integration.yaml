schema_version: "0.1"
id: work-tracker
kind: integration
transport: api
version: "1.0.0"
capabilities:
  - work.read
adapter_options:
  api_key: raw-secret-value-should-fail
permissions:
  write_requires: explicit-authority
health:
  required: authenticated
